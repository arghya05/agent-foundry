[pytest]
testpaths = tests
markers =
    integration: slower tests that talk to a real external process (npx MCP server, live A2A/uvicorn server, etc.) — still run by default, just not the ones to reach for first while iterating.
filterwarnings =
    ignore::DeprecationWarning
