"""Core — the framework-facing seam: Agent, Workflow, ExecutionContext, RunResult,
and the WorkflowEngine protocol LangGraph now sits behind instead of being the
framework itself. See agent_foundry/__init__.py for what's re-exported at the
top level.
"""
